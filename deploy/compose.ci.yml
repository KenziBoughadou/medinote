# Réseau et volume isolés par le nom de projet unique du runner CI.
services:
  api:
    environment:
      MEDINOTE_ENV: test
      MEDINOTE_LIVE_ENABLED: "false"
networks:
  private:
    ipam:
      config:
        - subnet: 172.30.73.0/24
