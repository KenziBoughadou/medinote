import { test, expect } from '@playwright/test'
import AxeBuilder from '@axe-core/playwright'
for(const width of [390,1024,1440])test(`clavier, axe et responsive ${width}`,async({page})=>{await page.setViewportSize({width,height:900});await page.goto('/?mode=offline');await expect(page.locator('.source-pane')).toBeVisible();if(width<1280){await page.getByRole('button',{name:'B · Extraction structurée',exact:true}).click();await expect(page.locator('#note-title-structured')).toBeVisible()};expect(await page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth)).toBe(true);const results=await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21aa']).analyze();expect(results.violations).toEqual([]);await page.keyboard.press('Tab');expect(await page.evaluate(()=>document.activeElement!==document.body)).toBe(true)})
